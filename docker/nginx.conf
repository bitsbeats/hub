server {
    listen 80;
    root /app;
    location / {
        try_files $uri $uri/ /index.html;
    }
    location /v2 {
        if ( $request_method !~ ^(GET|DELETE)) {
            return 403;
        }
        proxy_pass https://registry.thobits.com;
    }
}
